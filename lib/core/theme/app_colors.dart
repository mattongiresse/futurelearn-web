import 'package:flutter/material.dart';

class AppColors {
  // Couleurs principales de FUTURELEAN AND SERVICE
  static const Color primary = Color(0xFF1B4F72);      // Bleu foncé professionnel
  static const Color primaryLight = Color(0xFF2874A6); // Bleu moyen
  static const Color secondary = Color(0xFF3498DB);    // Bleu clair
  static const Color accent = Color(0xFF2ECC71);       // Vert confiance (optionnel)

  // Neutres
  static const Color background = Color(0xFFF8F9FA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color textPrimary = Color(0xFF1C2833);
  static const Color textSecondary = Color(0xFF5D6D7E);
  static const Color textLight = Color(0xFF85929E);

  // États
  static const Color success = Color(0xFF27AE60);
  static const Color error = Color(0xFFE74C3C);
  static const Color warning = Color(0xFFF39C12);

  // Autres
  static const Color divider = Color(0xFFD5D8DC);
  static const Color shadow = Color(0x1A000000);
}